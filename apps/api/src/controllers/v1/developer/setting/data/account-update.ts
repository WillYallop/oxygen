import express, { Request, Response, NextFunction } from 'express';

// * Description
/*  

*/

const accountUpdate = (req: Request, res: Response, next: NextFunction) => {};

export default accountUpdate;
