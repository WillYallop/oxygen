import express, { Request, Response, NextFunction } from 'express';

// * Description
/*  

*/

const profileUpdate = (req: Request, res: Response, next: NextFunction) => {};

export default profileUpdate;
