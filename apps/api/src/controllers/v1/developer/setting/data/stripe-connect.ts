import express, { Request, Response, NextFunction } from 'express';

// * Description
/*  

*/

const stripeConnect = (req: Request, res: Response, next: NextFunction) => {};

export default stripeConnect;
