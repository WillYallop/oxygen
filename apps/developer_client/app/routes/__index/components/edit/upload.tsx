// Components
import Header from '~/components/Layout/Header';

const UploadComponentPage: React.FC = () => {
    return (
        <>
            <Header hasSearch={false} />
            <h2>Upload Component Page</h2>
        </>
    );
};

export default UploadComponentPage;
